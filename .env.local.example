# Stripe secret key used by the /api/checkout route to create Checkout Sessions.
# Get one at https://dashboard.stripe.com/apikeys (use a test key for development).
STRIPE_SECRET_KEY=sk_test_your_key_here
